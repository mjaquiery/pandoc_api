setup:
  addons:
    - plan: heroku-postgresql
      as: db
    - plan: herok-redis
      as: redis

build:
  docker:
    web: web/Dockerfile

run:
  web: /web/init.sh
  worker: celery --app=pandoc_api worker --hostname=worker@%h --loglevel=INFO
